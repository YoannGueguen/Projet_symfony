{% extends 'base.html.twig' %}

{% block body %}
    <h1>Charge edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Modifier charge" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('charge_index') }}">Retour aux charges</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer charge">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
