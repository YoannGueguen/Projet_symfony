{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création discussion</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer discussion" class="btn btn-success" />
    {{ form_end(form) }}
{% if is_granted('ROLE_MANAGER') %}
    <ul style="list-style-type: none">
        <li>
            <a href="{{ path('discussion_index') }}"><button type="button" class="btn btn-primary">Retour aux discussions</button></a>
        </li>
    </ul>
    {% endif %}
{% endblock %}
