{% extends 'base.html.twig' %}

{% block body %}
    <h1>User</h1>

    <table class="table" >
        <tbody>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>E-mail</th>
            </tr>
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.username }}</td>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>

    <ul style="list-style-type: none">
        <li>
            <a href="{{ path('user_index') }}">
                <button type="button" class="btn btn-primary">Retour à la liste des utilisateurs</button>
            </a>
        </li>
        <li>
            <a href="{{ path('user_edit', { 'id': user.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
