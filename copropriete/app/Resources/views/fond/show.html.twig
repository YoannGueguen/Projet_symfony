{% extends 'base.html.twig' %}

{% block body %}
    <h1>Fond</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fond.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ fond.titre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fond_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fond_edit', { 'id': fond.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
