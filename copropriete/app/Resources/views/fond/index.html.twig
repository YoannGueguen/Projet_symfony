{% extends 'base.html.twig' %}

{% block body %}
    <h1>Fonds list</h1>

    <table>
        <thead>
            <tr>
                <th>Titre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fond in fonds %}
            <tr>
                <td><a href="{{ path('fond_show', { 'id': fond.id }) }}">{{ fond.titre }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('fond_show', { 'id': fond.id }) }}">Afficher</a>
                        </li>
                        <li>
                            <a href="{{ path('fond_edit', { 'id': fond.id }) }}">Modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fond_new') }}">Créer un nouveau fond</a>
        </li>
    </ul>
{% endblock %}
